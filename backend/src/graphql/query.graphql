# external definitions
# user.graphql: [UserResult, UsersResult]

type Query {
  getUser(email: String!): UserResult
  getUsers: UsersResult
  getProject(id: Int!): ProjectResult
  getIssue(id: Int!): IssueResult
}
