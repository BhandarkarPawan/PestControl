# external definitions
# user.graphql: [UserResult, UsersResult]

type Query {
  getUser(id: ID!): UserResult
  searchUsers(input: UserAddInput): UsersResult

  getProject(id: ID!): ProjectResult
  searchProjects(input: ProjectAddInput): ProjectsResult

  getIssue(id: ID!): IssueResult
  searchIssues(input: IssueSearchInput): IssuesResult
}
